# KaiYan 文档约定

- 这是 Mintlify 文档源目录，配置入口是 `docs.json`，页面使用 MDX。
- 文档面向 KaiYan 用户，默认使用中文；专有名词、命令、路径和代码标识符保留原文。
- 只记录已由产品或源码确认的公开能力，不猜测未发布的接口和限制。
- 不写入凭据、私有后端实现、生产基础设施细节或其他业务代码。
- 保持页面短小、主动语态和清晰的步骤顺序；UI 文案使用粗体，命令、路径和文件名使用代码格式。
- 使用 `mint dev` 做本地预览，使用 `mint validate` 做发布前校验。
